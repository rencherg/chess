game_data Table
id white_id black_id gamename bame

CREATE TABLE game_data (
    id INT AUTO_INCREMENT PRIMARY KEY,
    black_id INT,
    white_id INT,
    game_name VARCHAR(100),
    game_data VARCHAR(100)
);

INSERT INTO game_data
(
    white_id,
    black_id,
    game_name,
    game_data
)
VALUES
('12345', '123456', 'Test Game', 'Test Data');

auth_data Table
id user_id username token

CREATE TABLE auth_data (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT,
    username VARCHAR(50),
    token VARCHAR(50)
);

INSERT INTO auth_data
(
    user_id,
    username,
    token
)
VALUES
('123456', 'test', 'test1234');

user_data Table
id username password email

CREATE TABLE user_data (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50),
    password VARCHAR(50),
    email VARCHAR(50)
);

INSERT INTO user_data
(
    username,
    password,
    email
)
VALUES
('username', 'password', 'email');


check if db exists:
SELECT *
FROM information_schema.tables
WHERE table_schema = 'chess'
AND table_name = 'auth_data';